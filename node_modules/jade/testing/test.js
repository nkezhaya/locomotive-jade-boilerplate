
/**
 * Module dependencies.
 */

var jade = require('./../lib/jade');

var locals = {
    first: 'tj'
  , last: 0
};

jade.renderFile(__dirname + '/test.jade', { locals: locals }, function(err, html){
  if (err) throw err;
  console.log(html);
});